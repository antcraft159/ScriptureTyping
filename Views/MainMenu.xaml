<UserControl x:Class="ScriptureTyping.Views.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Left"
             VerticalAlignment="Stretch"
             ClipToBounds="False">

    <Grid Background="{x:Null}">
        <Border x:Name="SlidePanel"
                Width="215"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="10"
                Margin="24">

            <Border.RenderTransform>
                <TranslateTransform X="-200"/>
            </Border.RenderTransform>

            <Border.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                To="0"
                                Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>

                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                To="-200"
                                Duration="0:0:0.18">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseIn"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <!-- 손잡이 두께 -->
                    <ColumnDefinition Width="44"/>
                </Grid.ColumnDefinitions>

                <!-- 메뉴 영역 -->
                <Grid Grid.Column="0" Margin="18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="메인 메뉴"
                               Foreground="{StaticResource Brush.Text}"
                               FontSize="18"
                               FontWeight="SemiBold"/>

                    <UniformGrid Grid.Row="2" Rows="5" Columns="1">
                        <Button Content="학습 시작" MinHeight="44" Margin="0,0,0,10"
                                Command="{Binding StartLearningCommand}"/>
                        <Button Content="게임" MinHeight="44" Margin="0,0,0,10"
                                Command="{Binding GamesCommand}"/>
                        <Button Content="기록/통계" MinHeight="44" Margin="0,0,0,10"
                                Command="{Binding RecordsCommand}"/>
                        <Button Content="설정" MinHeight="44" Margin="0,0,0,10"
                                Command="{Binding SettingsCommand}"/>
                        <Button Content="종료" MinHeight="44"
                                Command="{Binding ExitCommand}"/>
                    </UniformGrid>
                </Grid>

                <!-- 손잡이 -->
                <Border Grid.Column="1"
                        Background="{StaticResource Brush.Surface2}"
                        BorderBrush="{StaticResource Brush.Border}"
                        BorderThickness="1,0,0,0">
                    <Grid>
                        <TextBlock Text="MENU"
                                   Foreground="{StaticResource Brush.Text2}"
                                   FontWeight="SemiBold"
                                   FontSize="11"
                                   RenderTransformOrigin="0.5,0.5"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center">
                            <TextBlock.RenderTransform>
                                <RotateTransform Angle="-90"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
